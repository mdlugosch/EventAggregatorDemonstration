<UserControl x:Class="EventAggregatorDemonstration.TargetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EventAggregatorDemonstration"
             xmlns:vm="clr-namespace:EventAggregatorDemonstration.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Border BorderBrush="Black" BorderThickness="1">
    <StackPanel Margin="10 0 10 0">
            <Label Content="" />
            <Label Content="Target"/>
        <TextBox x:Name="tbx_Target" Text="{Binding MessageText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Height="23" VerticalAlignment="Top" Width="120"/>
    </StackPanel>
    </Border>
</UserControl>
